package com.example.springbootjparelations.data.jointable;

import org.springframework.data.jpa.repository.JpaRepository;

public interface JoinSpaceShipRepository extends JpaRepository<JoinSpaceShip, Long> {
}
